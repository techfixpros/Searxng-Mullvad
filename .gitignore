# Local runtime state -- never commit real deployment data
mullvad.conf
history.db
country-index
active-mullvad.env
docker-compose.yml
core-config/
*.log

# Editor/OS junk
.DS_Store
*.swp
*~

# zsh completion cache, if compiled in place
*.zwc
